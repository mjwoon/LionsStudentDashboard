[project]
name = "ai-worker"
version = "0.1.0"
description = "AI Worker - 비동기 일괄 평가 및 AI 총평 생성"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "celery[redis]>=5.6.2",
    "openai>=2.21.0",
    "psycopg[binary]>=3.1.0",
    "sqlalchemy>=2.0.46",
]
