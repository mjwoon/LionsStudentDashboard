server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # React Router SPA 지원: 모든 경로를 index.html로 fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
